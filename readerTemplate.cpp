#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s;

    while (cin >> s)
    {
        if (s == "CHEIA")
        {
            // call function isCheia
        }
        else if (s == "INSERIR")
        {
            // call function inserir
        }
    }
    cout << endl;
    return 0;
}
